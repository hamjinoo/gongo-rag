# 02-gongo-rag 의존성
# 설치: pip install -r requirements.txt

# --- 문서 입력 ---
PyMuPDF>=1.26,<2            # 일반 PDF 텍스트 추출 + 스캔 페이지 이미지 변환
python-docx>=1.2,<2         # DOCX 문단·표 추출
Pillow>=11,<13              # 이미지 읽기
pytesseract>=0.3.13,<1     # Tesseract OCR 연결 (실행 엔진은 별도 설치)

# --- 기본 연산 ---
numpy>=2.1,<3
psutil>=7,<8                # reranker 모델 로드·추론 프로세스 메모리 측정

# --- 한국어 의미 검색·재정렬 ---
sentence-transformers>=5.6,<6  # 로컬 embedding·CrossEncoder 모델 실행
langchain-core>=1.4,<2         # Document와 Embeddings 표준 인터페이스
langchain-huggingface>=1.2,<2  # HuggingFaceEmbeddings 연결
langchain-chroma>=1.1,<2       # LangChain용 Chroma vector store
chromadb>=1.5,<2               # 로컬 vector DB

# --- 답변 생성 ---
openai>=1.40                   # LLM 답변 생성용 (또는 anthropic — rag_answer.py 주석 참고)

# --- 한국어 BM25 ---
kiwipiepy>=0.23,<1          # 조사·어미를 분리하는 한국어 형태소 분석

# --- 데모 UI ---
streamlit>=1.36

# --- 테스트 (선택 — 테스트 파일은 pytest 없이 python으로도 실행됨) ---
# pytest>=8.0
