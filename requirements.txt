# 02-gongo-rag 의존성
# 설치: pip install -r requirements.txt
# (아래 주석 처리된 것들은 해당 주차에 주석 풀고 다시 설치)

# --- 문서 입력 ---
PyMuPDF>=1.26,<2            # 일반 PDF 텍스트 추출 + 스캔 페이지 이미지 변환
python-docx>=1.2,<2         # DOCX 문단·표 추출
Pillow>=11,<13              # 이미지 읽기
pytesseract>=0.3.13,<1     # Tesseract OCR 연결 (실행 엔진은 별도 설치)

# --- Week 1부터 ---
numpy>=1.26

# --- Week 4부터 (임베딩 검색) ---
sentence-transformers>=3.0   # 문장 임베딩. 최초 실행 시 모델 ~400MB 자동 다운로드
openai>=1.40                 # LLM 답변 생성용 (또는 anthropic — rag_answer.py 주석 참고)

# --- Week 6부터 (한국어 형태소 분석 실험) ---
# kiwipiepy>=0.18

# --- 데모 UI ---
streamlit>=1.36

# --- 테스트 (선택 — 테스트 파일은 pytest 없이 python으로도 실행됨) ---
# pytest>=8.0
