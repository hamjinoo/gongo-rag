# 검색 실험 가이드

이 문서는 “다음에 무엇을 바꾸고, 결과에서 무엇을 봐야 하는지”만 빠르게 확인하는 체크리스트입니다.

자세한 개념은 [`docs/EVALUATION.md`](../docs/EVALUATION.md), 현재 실제 결과는
[`retrieval-evaluation-dev.md`](./retrieval-evaluation-dev.md)에서 확인합니다.

## 현재 기준선

- 문서: `docs/text`의 TXT 3개
- 청킹: paragraph, 700자, overlap 120자
- 검색: BM25 / Chroma / RRF / Reranker
- 평가 질문: dev normal 20개로 선택, test normal 10개로 최종 확인
- 지표: Hit@1·3·5, MRR, nDCG@1·3·5, 평균·p95 지연 시간
- 현재 결론: 후보 7개가 Hit@1 0.85, 평균 약 4.20초로 현재 dev 기준 최선입니다.

## 실험할 때 지킬 규칙

1. 설정을 고르는 동안에는 `dev` 질문만 사용합니다.
2. 한 번에 한 가지만 바꿉니다.
3. 비교할 때 문서, 청크, 질문, `k`는 같게 유지합니다.
4. 정확도와 속도를 함께 기록합니다.
5. 잘 안 된 질문도 지우지 말고 원인을 적습니다.
6. 최종 설정을 고른 뒤에만 `test`를 한 번 실행합니다.

`test`를 자꾸 보면 시험 답을 외운 뒤 실력이 늘었다고 착각하는 것과 같습니다.

## 다음 실험 순서

### 1. Reranker 후보 수 줄이기 — 완료

후보 10·7·5개를 비교했습니다.

- 5개: 약 48.2% 빨라졌지만 정답 2개를 후보에서 잃어 폐기
- 7개: Hit@1 0.85, MRR 0.925를 기록하고 약 33.1% 빨라져 채택
- 자세한 결과: [`reranker-candidate-comparison-dev.md`](./reranker-candidate-comparison-dev.md)

```powershell
.venv\Scripts\python.exe src\run_retrieval_evaluation.py `
  --split dev `
  --systems reranker `
  --ks 1,3,5 `
  --rerank-candidates 5
```

후보 5개로 Hit@10을 요청하면 실제 구현은 결과 10개를 만들기 위해 후보도 10개로
늘려야 합니다. CLI는 이제 이런 모순된 설정을 거부합니다.

### 2. 더 작은 로컬 Reranker 비교하기 — 완료

568M BGE와 118M MiniLM을 후보 7개 조건으로 비교했습니다.

- MiniLM: 평균 460.6ms로 BGE 4513.5ms보다 약 9.8배 빠름
- 품질: Hit@1 0.85 → 0.70, Hit@5 1.00 → 0.95로 하락
- 결정: 품질 우선 기본값은 BGE 유지, MiniLM은 속도 우선 선택지
- 자세한 결과: [`reranker-model-comparison-dev.md`](./reranker-model-comparison-dev.md)

### 3. Cohere Rerank 연결하기 — adapter 완료, 실제 비교 보류

관리형 API와 로컬 모델의 운영 차이를 설명할 수 있어 비교하기로 결정했습니다.
`CohereRerankScorer`를 기존 `PairScorer`에 맞춰 연결했고 가짜 API 응답 테스트를
통과했습니다.

- 실제 비교 항목: 검색 품질, API 응답 시간, billed search units, 문서 외부 전송
- 조건: 후보 7개, dev normal 20문항, `rerank-v4.0-pro`
- 보류 이유: `COHERE_API_KEY`가 없고 현재 공개 포트폴리오는 로컬 BGE로 완성 가능
- 주의: 무료 평가 키의 rate limit이 latency에 영향을 줄 수 있음

```powershell
Copy-Item .env.example .env
# .env 파일에서 COHERE_API_KEY 값만 채우기
.venv\Scripts\python.exe src\run_retrieval_evaluation.py `
  --split dev `
  --systems reranker `
  --ks 1,3,5 `
  --reranker-provider cohere `
  --rerank-candidates 7 `
  --output experiments\reranker-model-cohere-dev.json `
  --markdown-output experiments\reranker-model-cohere-dev.md
```

### 4. 설정 잠그고 test 한 번 실행하기 — 완료

`local BGE + 후보 7개`를 고정하고 test normal 10문항을 한 번 실행했습니다.

| 검색 방식 | Hit@1 | Hit@3 | Hit@5 | MRR | 평균 지연 |
|---|---:|---:|---:|---:|---:|
| BM25 | 0.80 | 0.90 | 0.90 | 0.833 | 0.7ms |
| Chroma | 0.50 | 0.70 | 0.90 | 0.633 | 20.1ms |
| RRF | 0.80 | 0.90 | 0.90 | 0.833 | 17.2ms |
| BGE Reranker | 0.80 | 0.90 | 0.90 | **0.850** | 4324.8ms |

자동 평가는 `q026`을 실패로 계산했지만 BM25 1위에 같은 제출 마감일이 있어
사람이 보면 답할 수 있었습니다. [최종 test 리포트](./retrieval-evaluation-test.md)에
수동 검토를 남겼습니다.

이제 test 결과에 맞춰 검색 설정을 다시 바꾸지 않습니다. 새 검색 설정을 실험하려면
기존 test를 튜닝용으로 사용하지 말고 새로운 holdout 질문을 만들어야 합니다.

### 다음 작업

검색 실험은 종료했습니다. 다음은 잠근 검색기를 LangGraph의 답변·재검색·거절
흐름에 연결하는 작업입니다.

## 실험 기록 템플릿

매번 별도 주간 기록을 만들 필요는 없습니다. 아래 항목만 PR 본문이나 새 실험
Markdown에 복사해서 사용합니다.

```text
가설:
바꾼 것:
그대로 둔 것:
실행 명령:
결과(정확도/속도):
실패 질문과 원인:
결정(채택/보류/폐기):
```

## 언제 청킹이나 토크나이저를 다시 바꾸나?

점수가 낮다는 이유만으로 모든 설정을 한꺼번에 바꾸지 않습니다. 실패 질문을
살펴봤을 때 아래 증거가 있을 때만 해당 단계를 다시 실험합니다.

- 정답 문장이 두 청크로 잘렸다면 청킹을 바꿉니다.
- 필요한 청크는 있으나 키워드 검색만 실패하면 한국어 토크나이저를 비교합니다.
- 후보에는 들어왔지만 1등이 아니면 Reranker를 개선합니다.
- 검색은 맞았는데 답변이 틀리면 생성과 Ragas 평가 단계에서 다룹니다.
